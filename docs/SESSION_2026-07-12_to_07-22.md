# Session summary — 2026-07-12 to 2026-07-22

A running record of the redesign work done across this stretch of sessions, in
order. Each item's commit hash links to the full diff and commit body (which
has more detail than this doc) — `git show <hash>` or check GitHub.

## Hero
- `3788e9a` Rebuilt the marquee as a GSAP bell-curve wave that morphs into a
  scroll-driven orbit ring around the headline, with a Glass Morphism 2.0
  unblur sweep on the upper arc.
- `3d1d19f` Swapped the six placeholder CDN images for real selected-works
  assets (posters for the three video disciplines).
- `7fc5f19` Replaced static hero posters with tiny looping video clips
  (128K/70K/79K) for Brand Strategy, Motion, and AI Automation — chosen over
  GIF for quality-per-KB. Also re-encoded the oversized Projects videos in
  place (20MB → 343K class savings; `public/projects` 39MB → 5MB total).

## New sections
- `6f4a6c5` **ProjectsSection** (designmonks.co-style alternating cards)
  replaced a 350vh sticky gallery. **Services** rebuilt from 8 full-screen
  rotating cards into an arounda/lessestudio-style interactive list + live
  preview. Combined effect: page scroll length 35 → ~25 screens.
- `743195b` Added **ManifestoSection** (scrubbed word-brighten statement) and
  **VelocityMarquee** (velocity-reactive outlined-type divider); unified
  heading reveals and added a shared `ParallaxMedia` drift primitive across
  Projects/Philosophy media.
- Earlier in the stretch: Philosophy rebuilt as an editorial "blog post" grid,
  the agency-comparison section rebuilt as a 3-way radar chart + detail
  matrix (agencies vs. freelancers vs. Solve Trend).

## Infrastructure
- `d6c5c6b` Reintroduced **Lenis smooth-scroll**, synced to GSAP's ticker and
  `ScrollTrigger.update` so the pinned hero and dark sequence stay locked
  (previously removed because it fought those pins — this time verified
  clean with `window.__lenis` in dev).
- `4abd88a` Fixed a **live-site-only bug**: local `/projects/*` asset paths
  in `<img>`/`<video>`/`next/image` don't get GitHub Pages' `/solve-trend`
  basePath automatically, so they 404'd in production while working fine
  locally. Added `lib/asset.ts` to prefix all local asset refs.
- Polish pass: hover-zoom media, magnetic CTAs, consistent word-rise heading
  reveals sitewide (`9e5bfa9`).

## Known open items (see also `docs/CONTENT_NEEDS.md`)
- **Client logos**: `Hero Section/Clients Logo/` is empty — the "trusted by"
  strip still shows Logoipsum placeholders. Drop real logos in when ready.
- No further known bugs as of `7fc5f19`. Live site was broken (basePath) as
  of this session's start; fixed and pushed — confirm it's resolved after
  the next GitHub Actions deploy.

## Verification standard used throughout
Every change in this log was checked in a real Chromium instance (Playwright)
— not just typecheck/build — because this site is animation-heavy and static
checks don't catch rendering regressions. The `e2e/site-behavior.spec.ts`
suite (10 tests, desktop + mobile) passed before every push in this log.
